# Rust
/target/
**/target/

# Generated by omarc and the reaction code generator, beside the program.
src-gen/
/lang/.lake/
Cargo.lock

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Test artifacts
*.log
junk/

# Node lockfile from tooling experiments; this is a Rust workspace.
/package-lock.json

# A sketch of language features that do not exist yet (parameterised teams, a
# `main` block). CI compiles every program in this directory, so tracking it
# breaks the build. Ignored so it cannot be swept in again by `git add -A`.
